# 🔒 Қауіпсіздік нұсқаулары

## ⚠️ Production-қа шығармас бұрын

### 1. Деректер базасының құпия сөздері

**❌ Жасамаңыз:**
\`\`\`properties
spring.datasource.password=mypassword123
\`\`\`

**✅ Дұрыс:**
\`\`\`properties
spring.datasource.password=${DB_PASSWORD}
\`\`\`

Содан кейін environment variable-ды орнатыңыз:
\`\`\`bash
export DB_PASSWORD=your_secure_password
\`\`\`

### 2. JWT Secret Key

**❌ Жасамаңыз:**
Секретті кілтті кодта қалдырмаңыз!

**✅ Дұрыс:**
\`\`\`properties
jwt.secret=${JWT_SECRET}
\`\`\`

Қауіпсіз секрет жасау:
\`\`\`bash
# Linux/Mac
openssl rand -base64 64

# Windows PowerShell
[Convert]::ToBase64String((1..64|%{Get-Random -Max 256}))
\`\`\`

### 3. CORS Configuration

Production-да тек қажетті домендерге рұқсат беріңіз:
\`\`\`java
@Configuration
public class CorsConfig {
    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addCorsMappings(CorsRegistry registry) {
                registry.addMapping("/api/**")
                    .allowedOrigins("https://yourdomain.com")
                    .allowedMethods("GET", "POST", "PUT", "DELETE");
            }
        };
    }
}
\`\`\`

### 4. HTTPS қолдану

Production-да міндетті түрде HTTPS қолданыңыз!

### 5. Rate Limiting

DDoS шабуылдардан қорғану үшін rate limiting қосыңыз.

### 6. Пароль талаптары

Қазіргі минимум: 6 символ
**Ұсынылады:** 
- Кемінде 8 символ
- Үлкен және кіші әріптер
- Цифрлар
- Арнайы символдар

## 🔐 Flutter қауіпсіздігі

### 1. API Keys

API кілттерін қауіпсіз сақтаңыз:
\`\`\`dart
// ❌ Жасамаңыз
const apiKey = "my-secret-key";

// ✅ Дұрыс - .env файлда
flutter_dotenv.env['API_KEY'];
\`\`\`

### 2. Secure Storage

Құпия деректерді \`flutter_secure_storage\` қолданып сақтаңыз:
\`\`\`dart
final storage = FlutterSecureStorage();
await storage.write(key: 'token', value: token);
\`\`\`

### 3. Certificate Pinning

Production-да SSL certificate pinning қосыңыз.

## 📋 Тексеру тізімі

- [ ] Барлық пароль ашық түрде сақталмайды
- [ ] JWT секреті environment variable-да
- [ ] Database credentials қорғалған
- [ ] HTTPS қолданылады
- [ ] CORS дұрыс конфигурацияланған
- [ ] Rate limiting қосылған
- [ ] Логтарда құпия деректер жоқ
- [ ] API эндпоинттер қорғалған
- [ ] Input validation бар
- [ ] Error messages құпия ақпарат көрсетпейді

## 🚨 Қауіпсіздік инциденті жағдайында

1. Дереу барлық құпия кілттерді өзгертіңіз
2. Барлық токендерді жарамсыз қылыңыз
3. Логтарды тексеріңіз
4. Қолданушыларды хабарландырыңыз
5. Инцидент туралы есеп жасаңыз

## 📞 Байланыс

Қауіпсіздік мәселесін тапсаңыз, дереу хабарлаңыз!


