# SemperMove - Фитнес-трекер с соревновательным элементом

## 📖 Что это за проект?

**SemperMove** - это мобильное приложение для отслеживания физической активности с уникальной функцией дуэлей (соревнований) между пользователями. Проект состоит из двух частей: мобильного приложения на Flutter и серверной части на Spring Boot.

### Основная идея
Пользователи могут отслеживать свою ежедневную физическую активность (шаги, отжимания, приседания, планку, потребление воды), следить за прогрессом через графики и статистику, а также вызывать других пользователей на дуэли - соревнования по конкретному виду упражнений.

## 🎯 Для кого этот проект?

- **Любители фитнеса**, которые хотят отслеживать свою активность
- **Соревновательные люди**, которым нужна мотивация через дуэли
- **Друзья и команды**, которые хотят соревноваться друг с другом
- **Люди, начинающие заниматься спортом**, которым нужна система мотивации

## 🏗️ Архитектура проекта

### Структура
```
SemperMove/
├── backend/           # Серверная часть (Spring Boot)
│   └── src/
│       └── main/
│           ├── java/  # Java код
│           └── resources/  # Конфигурация
├── frontend/          # Мобильное приложение (Flutter)
│   └── lib/
│       ├── models/    # Модели данных
│       ├── screens/   # Экраны приложения
│       └── services/  # API сервисы
└── документация       # Файлы README, QUICKSTART и т.д.
```

### Backend (Серверная часть)

**Технологии:**
- **Spring Boot 3.5.6** - основной фреймворк
- **Java 17** - язык программирования
- **PostgreSQL** - база данных для хранения пользователей, прогресса, дуэлей
- **Spring Security + JWT** - аутентификация и безопасность
- **BCrypt** - шифрование паролей
- **JPA/Hibernate** - работа с базой данных

**Структура backend:**

1. **Models (Модели данных):**
   - `User` - пользователь с email, username, паролем и текущим прогрессом
   - `Duel` - дуэль между двумя пользователями (челленджер и оппонент)
   - `ProgressHistory` - история прогресса по дням

2. **Controllers (API endpoints):**
   - `UserController` - регистрация, вход, список пользователей
   - `ProgressController` - получение и обновление текущего прогресса
   - `DuelController` - создание, управление и завершение дуэлей
   - `ProgressHistoryController` - работа с историей активности

3. **Security (Безопасность):**
   - `JwtUtil` - генерация и валидация JWT токенов
   - `JwtAuthenticationFilter` - фильтр для проверки токенов
   - `UserDetailsServiceImpl` - загрузка данных пользователя
   - `SecurityConfig` - настройка Spring Security
   - `CorsConfig` - настройка CORS для мобильного приложения

4. **Services:**
   - `UserService` - бизнес-логика работы с пользователями

5. **Repositories:**
   - `UserRepository` - доступ к данным пользователей
   - `DuelRepository` - доступ к данным дуэлей
   - `ProgressHistoryRepository` - доступ к истории

**Как работает backend:**

1. Пользователь отправляет запрос на регистрацию
2. Сервер шифрует пароль с помощью BCrypt
3. Создается запись в базе данных
4. При входе сервер проверяет email и пароль
5. Генерируется JWT токен (действует 24 часа)
6. Токен отправляется клиенту
7. Все последующие запросы содержат этот токен в заголовке Authorization
8. Фильтр проверяет токен и идентифицирует пользователя

**База данных:**
- Таблица `users` - хранит пользователей и их текущую активность
- Таблица `duels` - хранит информацию о дуэлях
- Таблица `progress_history` - хранит историю активности по дням
- Таблица `user_friends` - связи между друзьями (many-to-many)

### Frontend (Мобильное приложение)

**Технологии:**
- **Flutter 3.9.2+** - фреймворк для кроссплатформенной разработки
- **Dart** - язык программирования
- **Provider** - управление состоянием
- **HTTP** - отправка запросов к API
- **FL Chart** - графики и статистика
- **Shared Preferences** - локальное хранение JWT токена

**Структура frontend:**

1. **Models:**
   - `User` - модель пользователя
   - `Duel` - модель дуэли
   - `ProgressHistory` - модель истории

2. **Services:**
   - `AuthService` - аутентификация (сохранение/получение токена)
   - `ApiService` - все запросы к backend API

3. **Screens (Экраны):**

   a) **Auth (Авторизация):**
   - `LoginScreen` - экран входа
   - `RegisterScreen` - экран регистрации

   b) **Home:**
   - `MainScreen` - главный экран с навигацией
   - `DashboardScreen` - дашборд с текущей активностью

   c) **Duels:**
   - `DuelsScreen` - список дуэлей (активные/история)
   - `CreateDuelScreen` - создание новой дуэли
   - `DuelDetailScreen` - детальный просмотр дуэли

   d) **Statistics:**
   - `StatisticsScreen` - графики и статистика

   e) **Profile:**
   - `ProfileScreen` - профиль пользователя

**Дизайн:**
- Темная тема (черный фон #0A0A0A)
- Яркий зеленый акцент (#00FF88)
- Минималистичный современный UI
- Плавные анимации
- Интерактивные элементы

## ⚙️ Как все работает (полный цикл)

### 1. Регистрация и вход

**Шаг 1:** Пользователь открывает приложение
- Появляется Splash Screen с логотипом
- Приложение проверяет наличие сохраненного токена

**Шаг 2:** Если токена нет - показывается экран входа
- Пользователь нажимает "Зарегистрироваться"
- Вводит email, username, пароль (минимум 6 символов)
- Данные отправляются на `POST /api/users/register`

**Шаг 3:** Backend обрабатывает регистрацию
- Проверяет, не занят ли email
- Шифрует пароль с помощью BCrypt
- Сохраняет в PostgreSQL
- Возвращает данные пользователя

**Шаг 4:** Пользователь входит
- Вводит email и пароль
- Данные отправляются на `POST /api/users/login`
- Backend проверяет пароль и генерирует JWT токен
- Токен сохраняется в Shared Preferences на телефоне

### 2. Отслеживание активности

**Dashboard экран:**
- Показывает 5 категорий активности:
  - 🚶 Шаги (цель: 10,000/день)
  - 💪 Отжимания
  - 🦵 Приседания
  - 🧘 Планка (в секундах)
  - 💧 Вода (в мл)

**Обновление прогресса:**
- Пользователь нажимает "+" рядом с категорией
- Вводит новое значение
- Данные отправляются на `POST /api/progress/update`
- Backend обновляет значения в таблице users
- Создается запись в progress_history для статистики

**История:**
- Каждое обновление сохраняется с датой
- Это позволяет строить графики за неделю/месяц
- `GET /api/history` возвращает все записи пользователя

### 3. Статистика

**Statistics экран:**
- Запрашивает историю через `GET /api/history`
- Получает массив записей по дням
- Строит графики с помощью FL Chart:
  - Линейные графики для шагов
  - Столбчатые графики для упражнений
  - График потребления воды

**Фильтры:**
- За последние 7 дней
- За последний месяц
- За все время

### 4. Дуэли (Соревнования)

**Создание дуэли:**

**Шаг 1:** Пользователь открывает экран Duels
- Нажимает "Создать дуэль"
- Выбирает оппонента из списка пользователей (кроме себя)
- Выбирает категорию упражнения (отжимания, приседания и т.д.)

**Шаг 2:** Создание дуэли на сервере
- Запрос отправляется на `POST /api/duels/start`
- Backend создает запись в таблице duels:
  ```
  challenger_id: текущий пользователь
  opponent_id: выбранный оппонент
  exercise_category: pushups/squats/etc.
  status: IN_PROGRESS
  challenger_score: 0
  opponent_score: 0
  ```

**Шаг 3:** Дуэль отображается в списке
- У обоих пользователей появляется в активных дуэлях
- Каждый может обновлять свой счет

**Обновление счета:**
- Пользователь вводит количество выполненных упражнений
- Отправляется на `POST /api/duels/{id}/update-scores`
- Backend обновляет challenger_score или opponent_score

**Завершение дуэли:**
- Любой участник может завершить дуэль
- Запрос `POST /api/duels/{id}/finish`
- Backend сравнивает счета
- Определяет победителя
- Меняет status на FINISHED
- Записывает winner

**Просмотр дуэли:**
- Экран показывает:
  - Имена участников
  - Категорию упражнения
  - Текущие счета
  - Кто победил (если завершена)

### 5. Профиль

**Profile экран показывает:**

**Информация о пользователе:**
- Username
- Email
- Аватар (иконка по умолчанию)

**Уровень и опыт:**
- Система уровней (Level 1, 2, 3...)
- XP рассчитывается из активности
- Прогресс-бар до следующего уровня

**Статистика дуэлей:**
- Всего дуэлей
- Побед
- Поражений
- Процент побед

**Достижения (бейджи):**
- 🏆 Первая дуэль
- 🔥 Серия побед
- 💪 1000 отжиманий
- 🚶 10,000 шагов
- И другие

**Logout:**
- Кнопка выхода
- Удаляет токен из Shared Preferences
- Перенаправляет на экран входа

## 🔐 Безопасность

### Backend Security

1. **Шифрование паролей:**
   - Используется BCrypt с автоматической солью
   - Пароль НИКОГДА не хранится в открытом виде
   - При проверке пароля сравниваются хеши

2. **JWT токены:**
   - При входе генерируется токен
   - Токен содержит: email пользователя, время создания
   - Подписан секретным ключом (хранится в переменных окружения)
   - Действует 24 часа
   - После истечения нужно войти заново

3. **Проверка запросов:**
   - Каждый защищенный endpoint требует токен
   - JwtAuthenticationFilter проверяет токен
   - Извлекает email из токена
   - Загружает пользователя из БД
   - Устанавливает аутентификацию в Spring Security

4. **CORS (Cross-Origin Resource Sharing):**
   - Разрешены запросы с любых источников (для мобильного приложения)
   - В production следует ограничить конкретными доменами

5. **Валидация данных:**
   - Email проверяется на формат
   - Username - обязательное поле
   - Пароль - минимум 6 символов
   - Проверка уникальности email

### Frontend Security

1. **Хранение токена:**
   - Используется Shared Preferences (безопасное хранилище)
   - На iOS - Keychain
   - На Android - EncryptedSharedPreferences

2. **Отправка токена:**
   - Каждый запрос включает заголовок:
     `Authorization: Bearer {token}`
   - Токен автоматически добавляется в ApiService

3. **Обработка ошибок:**
   - Если токен истек - показывается ошибка "Invalid token"
   - Пользователь перенаправляется на экран входа
   - Токен удаляется

## 📊 База данных (PostgreSQL)

### Таблица users
```sql
id              BIGSERIAL PRIMARY KEY
email           VARCHAR UNIQUE NOT NULL
username        VARCHAR
password        VARCHAR NOT NULL  -- BCrypt хеш
daily_steps     INTEGER DEFAULT 0
push_ups        INTEGER DEFAULT 0
squats          INTEGER DEFAULT 0
plank_seconds   INTEGER DEFAULT 0
water_ml        INTEGER DEFAULT 0
```

### Таблица duels
```sql
id                  BIGSERIAL PRIMARY KEY
challenger_id       BIGINT (FK -> users)
opponent_id         BIGINT (FK -> users)
status              VARCHAR ('IN_PROGRESS', 'FINISHED')
winner              VARCHAR
exercise_category   VARCHAR
challenger_score    INTEGER
opponent_score      INTEGER
exercises           TEXT (JSON для множественных категорий)
created_at          TIMESTAMP
updated_at          TIMESTAMP
```

### Таблица progress_history
```sql
id              BIGSERIAL PRIMARY KEY
user_id         BIGINT
date            DATE
steps           INTEGER
push_ups        INTEGER
squats          INTEGER
plank_seconds   INTEGER
water_ml        INTEGER
```

### Таблица user_friends (many-to-many)
```sql
user_id    BIGINT (FK -> users)
friend_id  BIGINT (FK -> users)
```

## 🚀 Как запустить проект?

### Требования
- Java 17+
- PostgreSQL 14+
- Flutter 3.9+
- Android Studio / Xcode (для эмулятора)

### Backend

1. **Создать базу данных:**
```bash
psql -U postgres
CREATE DATABASE sempermove;
\q
```

2. **Настроить application.properties:**
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/sempermove
spring.datasource.username=postgres
spring.datasource.password=your_password
jwt.secret=your_super_secret_key_min_256_bits
```

3. **Запустить:**
```bash
cd backend
./mvnw spring-boot:run
```

Сервер запустится на http://localhost:8080

### Frontend

1. **Установить зависимости:**
```bash
cd frontend
flutter pub get
```

2. **Настроить API URL (для Android эмулятора):**
В `lib/services/api_service.dart`:
```dart
static const String baseUrl = 'http://10.0.2.2:8080/api';
```

Для iOS симулятора: `http://localhost:8080/api`
Для реального устройства: `http://YOUR_COMPUTER_IP:8080/api`

3. **Запустить:**
```bash
flutter run
```

## 📱 API Endpoints (полный список)

### Аутентификация
- `POST /api/users/register` - Регистрация
- `POST /api/users/login` - Вход
- `GET /api/users/all` - Список всех пользователей (требует токен)

### Прогресс
- `GET /api/progress` - Получить текущий прогресс (требует токен)
- `POST /api/progress/update` - Обновить прогресс (требует токен)

### Дуэли
- `POST /api/duels/start` - Создать дуэль (требует токен)
- `GET /api/duels/active` - Активные дуэли (требует токен)
- `GET /api/duels/history` - История дуэлей (требует токен)
- `GET /api/duels/{id}` - Детали дуэли (требует токен)
- `POST /api/duels/{id}/update-scores` - Обновить счет (требует токен)
- `POST /api/duels/{id}/finish` - Завершить дуэль (требует токен)

### История
- `GET /api/history` - История прогресса (требует токен)
- `POST /api/history/add` - Добавить запись (требует токен)

## 🎨 Дизайн и UI/UX

### Цветовая схема
- **Основной фон:** #0A0A0A (глубокий черный)
- **Карточки:** #1A1A1A (темно-серый)
- **Границы:** #2A2A2A (серый)
- **Акцент:** #00FF88 (неоновый зеленый)
- **Текст:** #FFFFFF (белый)
- **Вторичный текст:** #888888 (серый)

### UI элементы
- Скругленные углы (12-16px)
- Плавные тени и градиенты
- Интерактивные кнопки с эффектами нажатия
- Прогресс-бары с анимацией
- Карточки с границами
- Иконки Material Design

### UX особенности
- Splash Screen при запуске
- Автоматическая авторизация
- Pull-to-refresh для обновления данных
- Loading индикаторы
- Понятные сообщения об ошибках
- Плавная навигация между экранами
- Подтверждение важных действий

## 📈 Функциональные возможности

### Реализовано ✅

1. **Система аутентификации**
   - Регистрация с валидацией
   - Вход с JWT токенами
   - Автоматический вход
   - Выход из аккаунта

2. **Отслеживание активности**
   - 5 категорий упражнений
   - Обновление в реальном времени
   - Сохранение истории

3. **Статистика**
   - Графики за неделю/месяц
   - Все категории упражнений
   - Интерактивные чарты

4. **Дуэли**
   - Создание дуэлей
   - Выбор оппонента и категории
   - Обновление счета
   - Определение победителя
   - История дуэлей

5. **Профиль**
   - Система уровней
   - Опыт (XP)
   - Статистика дуэлей
   - Достижения

### Планируется 📋

1. **AI интеграция**
   - Распознавание упражнений через камеру
   - Подсчет повторений
   - Проверка техники

2. **Социальные функции**
   - Добавление друзей
   - Чат
   - Комментарии
   - Лайки

3. **Расширенные функции**
   - Push уведомления
   - Тренировочные планы
   - Таймеры для упражнений
   - Достижения и бейджи
   - Рейтинговая таблица
   - Командные дуэли

## 📚 Документация

- **README.md** - Этот файл (общее описание)
- **QUICKSTART.md** - Быстрый старт и инструкции
- **API_EXAMPLES.md** - Примеры API запросов с curl
- **SECURITY.md** - Рекомендации по безопасности
- **CHANGELOG.md** - История изменений

## 🎯 Для разработчиков

### Структура кода

**Backend:**
- Следует стандартам Spring Boot
- Используется паттерн MVC
- Разделение на слои: Controller -> Service -> Repository
- Все модели с JPA аннотациями
- Конфигурация в отдельных классах

**Frontend:**
- Следует рекомендациям Flutter
- Разделение на models, services, screens
- Переиспользуемые виджеты
- Async/Await для асинхронных операций
- Обработка ошибок с try-catch

### Как добавить новую функцию?

**Backend:**
1. Создать модель (если нужна новая таблица)
2. Создать repository
3. Создать service с бизнес-логикой
4. Создать controller с endpoints
5. Добавить в SecurityConfig (если нужна авторизация)

**Frontend:**
1. Создать модель (если нужна)
2. Добавить методы в ApiService
3. Создать экран (если нужен)
4. Добавить в навигацию
5. Стилизовать согласно дизайну

## 🐛 Отладка и тестирование

### Тестирование API
```bash
# Регистрация
curl -X POST http://localhost:8080/api/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","username":"Test","password":"123456"}'

# Вход
curl -X POST http://localhost:8080/api/users/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'

# Получить прогресс
curl -X GET http://localhost:8080/api/progress \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Логи
- **Backend:** Логи в консоли Spring Boot
- **Frontend:** `print()` выводит в консоль Flutter
- **Database:** `spring.jpa.show-sql=true` показывает SQL запросы

## 💡 Советы по использованию

1. **Для пользователей:**
   - Обновляйте прогресс ежедневно
   - Участвуйте в дуэлях для мотивации
   - Проверяйте статистику для отслеживания прогресса
   - Повышайте уровень через активность

2. **Для разработчиков:**
   - Используйте переменные окружения для секретов
   - Не коммитьте application.properties с реальными данными
   - Тестируйте на разных устройствах
   - Проверяйте безопасность перед production

## 📞 Поддержка и вопросы

Если возникли проблемы:
1. Проверьте [QUICKSTART.md](QUICKSTART.md) - там решения частых проблем
2. Проверьте [SECURITY.md](SECURITY.md) - рекомендации по безопасности
3. Изучите [API_EXAMPLES.md](API_EXAMPLES.md) - примеры запросов

## 📊 Статистика проекта

- **Код:** ~5000+ строк
- **Файлов:** 30+ Java/Dart файлов
- **API endpoints:** 14
- **Экранов:** 7
- **Моделей данных:** 3
- **Время разработки:** 1.0 версия

## 🎉 Заключение

SemperMove - это полнофункциональное приложение для отслеживания фитнес-активности с уникальной системой дуэлей. Проект готов к использованию, имеет современный дизайн, безопасную аутентификацию и масштабируемую архитектуру.

**Статус проекта:** ✅ Production Ready (с учетом рекомендаций по безопасности)

**Версия:** 1.0.0

**Лицензия:** Указать вашу лицензию

---

*"Semper Move" - Всегда в движении к победе!* 💪
